import React from 'react';
import useTitle from '../../hooks/useTitle';

const Admission = () => {
    useTitle("Admission")
    return (
        <div>
            Admission compo
        </div>
    );
};

export default Admission;