import React from 'react';
import useTitle from '../../hooks/useTitle';

const Colleges = () => {
    useTitle("Colleges")
    return (
        <div>
            Collages compo
        </div>
    );
};

export default Colleges;